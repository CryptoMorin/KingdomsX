enabled: bool
limit: Math

disable-shift-click: bool
remove-unclaimed: bool
to-inventory-on-break: bool

teleportation-invulnerability:
  (type): int
  min: 1

disallow-accidental-mob-damage:
  friendly:
    (type): set
    (elements): Enum<org.bukkit.entity.EntityType>
  others:
    (type): set
    (elements): Enum<org.bukkit.entity.EntityType>

temporary-hologram-display: Period

update-ticks:
  (type): int
  min: 1

