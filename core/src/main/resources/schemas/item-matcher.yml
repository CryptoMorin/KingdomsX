name: StringMatcher
lore: StringMatcher
material: StringMatcher<Material>
custom-model-data: int

enchants:
  (keys): Enchant
  (values):
    - [ '*' ]
    - int
    - Condition

nbt:
  (values): StringMatcher
