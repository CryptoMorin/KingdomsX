persistent-data: bool
update-interval: Period
priority: int
boost: bool
hide-by-default: bool

zoom:
  max: int
  min: int

flag:
  cooldown: Period
  
  size-limit:
    width: int
    height: int
  
  links:
    blacklist: bool
    contains:
      (type): list
      (elements): str
    regex:
      (type): list
      (elements): str

fill:
  opacity: int
  color: str

line:
  weight: int
  color: str

invasion:
  fill:
    opacity: int
    color: str
  
  line:
    weight: int
    opacity: int
    color: str

description:
  flag: str
  main: str
  members: str
  allies: str
  truces: str
  enemies: str

icons:
  enabled: bool
  
  zoom:
    max: int
    min: int
  
  home: str
  national-spawn: str
  national-nexus: str
  nexus: str
  powercell: str
  extractor: str
  outpost: str
  warppad: str
  regulator: str