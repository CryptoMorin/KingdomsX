powerups:
  (keys): [ 'damage-boost', 'damage-reduction', 'arrow-boost', 'regeneration-boost' ]
  (values):
    enabled: bool
    name: str
    cost: Math
    scaling: Math
    max-level: Math
    own-land-only: bool

power:
  enabled: bool
  player:
    max: decimal
    min: decimal
    initial: decimal
    
    regeneration:
      every: str
      charge: int
    
    loss:
      death: decimal
      offline:
        every: str
        lose: decimal
        min: decimal
  faction:
    max: int