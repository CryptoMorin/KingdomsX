powerups:
  (keys): [ 'damage-boost', 'damage-reduction', 'arrow-boost', 'regeneration-boost' ]
  (values):
    enabled: bool
    cost: Math
    scaling: Math
    max-level: Math
    own-land-only: bool

power:
  enabled: bool
  player:
    max: decimal
    min: decimal
    initial: decimal

    regeneration:
      every: Period
      charge: decimal

    loss:
      death: decimal
      offline:
        every: Period
        lose: decimal
        min: decimal
  faction:
    max: decimal