(x-miscUpgrades): &miscUpgrades [ invasions, invasion-teleportation, anti-explosion, anti-trample, nexus-guards, guards, max-members, max-claims, chest-size, glory, insanity ]

(required): *miscUpgrades
(keys): *miscUpgrades
(values):
  enabled: bool
  name: str
  scaling: Math
  cost: Math
  refresh-cooldown: Math
  max-level: Math
  default-level: Math
  effects:
    (required): [ '1' ]
    (keys): int
    (values):
      (type): list
      (elements): Potion