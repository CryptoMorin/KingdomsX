channels:
  (keys): [ global, kingdom, truce, ally, nation, direct-prefix ]
  direct-prefix: str
  (values):
    name: str
    short: str
    color: str

priority: str
cancel: bool
recipients:
  respect-others: bool
  clear-when-done: bool

reset-channel-on-leave: bool
ranged-channels:
  channels:
    (type): list
    (elements): str
  radius: int
  bypass-prefix: str

tagging:
  enabled: bool
  prefix: str

global-channel:
  format: bool
  parse-as-placeholder: bool
  relational-placeholders:
    color: str
    name: str

discordsrv:
  global-channel: str
  private-channel: str
  announcements:
    invasion:
      channel: str
      message: str