(keys): [ invasions, invasion-teleportation, anti-explosion, anti-trample, nexus-guards, guards, max-members, max-claims, chest-size, glory, insanity ]
(values):
  enabled: bool
  name: str
  scaling: Math
  cost: Math
  refresh-cooldown: Math
  max-level: int
  default-level: int
