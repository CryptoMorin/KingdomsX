powerups:
  damage-boost:
    enabled: bool
    name: str
    cost: str
    scaling: str
    max-level: int
    own-land-only: bool
  damage-reduction:
    enabled: bool
    name: str
    cost: str
    scaling: str
    max-level: int
    own-land-only: bool
  arrow-boost:
    enabled: bool
    name: str
    cost: str
    scaling: str
    max-level: int
    own-land-only: bool
  regeneration-boost:
    enabled: bool
    name: str
    cost: str
    scaling: str
    max-level: int
    own-land-only: bool

power:
  enabled: bool
  player:
    max: decimal
    min: decimal
    initial: decimal
    
    regeneration:
      every: str
      charge: int
    
    loss:
      death: decimal
      offline:
        every: str
        lose: decimal
        min: decimal
  faction:
    max: int